# Optional SMTP settings. If not provided, the server will use an Ethereal test account for development (no real emails sent).
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_pass
# SMTP_SECURE=false

FROM_EMAIL=sender@example.com
TO_EMAIL=ahhzuku@gmail.com
EMAIL_SUBJECT=Website submission
PORT=3000

# Admin token used to protect endpoints that can email all subscribers.
# Set a secret value and keep it private - required to call /subscribers and /admin/announce
# ADMIN_TOKEN=change_me_to_a_random_secret
